<?xml version='1.0' encoding='UTF-8'?>
<jboss-deployment-structure xmlns="urn:jboss:deployment-structure:1.2">
    <!-- Make sub deployments isolated by default, so they cannot see each others classes without a Class-Path entry -->
    <ear-subdeployments-isolated>false</ear-subdeployments-isolated>
    <deployment>
        <exclusions>
            <!-- The application is compiled and packaged with Hibernate ORM 7.2.1. -->
            <module name="org.hibernate"/>
            <module name="org.hibernate.envers"/>
        </exclusions>
        <dependencies>
            <!-- Externalized eGov runtime properties. Application libraries are packaged in the EAR. -->
            <module name="jdk.xml.dom" export="true"/>
            <module name="org.hibernate.validator" export="true"/>
            <module name="org.egov.settings" export="true" optional="true"/>
        </dependencies>
    </deployment>
</jboss-deployment-structure>
