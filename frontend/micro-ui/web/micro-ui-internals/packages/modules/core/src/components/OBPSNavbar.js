import React, {useState} from "react";
// import NestedDropdown from "./NestedDropdown";

const OBPSNavbar = ({ items = [] }) => {
  console.log("NavbarItems", items)

  return (
    <React.Fragment>

    </React.Fragment>
  );
};

export default OBPSNavbar;